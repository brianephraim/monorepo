/* eslint-disable no-undef */
/* eslint-disable no-console */
/* eslint-disable prefer-template */
import reduxRoutingAppRootComponent from './redux-routing-app-root-component';

console.log('logging from redux-routing-app-root-component', reduxRoutingAppRootComponent);
test('adds 1 + 2 to equal 3', () => { expect(1 + 2).toBe(3); });
