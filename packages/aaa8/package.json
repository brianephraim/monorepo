{
  "name": "@defualt/aaa8",
  "version": "0.0.1",
  "publishConfig": {
    "access": "public"
  },
  "scripts": {
    "start": "devenv",
    "thing": "devenv --env=build --dirroot=$(pwd)",
    "thing1": "devenv --env=node --dirroot=$(pwd)"
  },
  "devDependencies": {
    "@defualt/dev_env": "^0.0.13"
  }
}
